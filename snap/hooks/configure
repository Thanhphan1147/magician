#!/bin/sh
set -e

BACKEND_USER="$(snapctl get backend.user)"

# Set default if not configured
if [ -z "$BACKEND_USER" ]; then
    snapctl set backend.user=ubuntu
fi

# Restart backend service to pick up the new user
snapctl restart magician.backend
